# Copy this file to .env and fill in your own values.
# Never commit .env (it's already in .gitignore).

# Required only if AGENT_DECISION_PROVIDER=anthropic.
ANTHROPIC_API_KEY=

# Which Claude model the Anthropic decision provider uses.
ANTHROPIC_MODEL=claude-opus-4-8

# "rule_based" (default, no external calls) or "anthropic" (asks Claude
# to pick a tool, with automatic fallback to rule_based on any failure).
AGENT_DECISION_PROVIDER=rule_based
